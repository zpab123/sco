syntax = "proto3";

package protocol;

import "grpcRequest.proto";
import "grpcResponse.proto";

// 定义一个 Grpc 服务,其中 API 为 Call
service Grpc {
	rpc HandlerCall(GrpcRequest) returns (GrpcResponse) {} // client->server 远程调用
	rpc Call(GrpcRequest) returns (GrpcResponse) {}
}