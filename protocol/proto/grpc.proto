syntax = "proto3";

package protocol;

import "handler.proto";
import "remote.proto";

// 定义一个 Sco rpc 服务
service Sco {
	rpc HandlerCall(HandlerReq) returns (HandlerRes) {} // client->server 远程调用
	rpc RemoteCall(RemoteReq) returns (RemoteRes) {} // server->server 远程调用
}